name: presence_etudiant_app
description: Application Android étudiante — connexion, scan QR, pointage présence.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  file_picker: ^8.1.6
  mobile_scanner: ^6.0.2
  shared_preferences: ^2.3.3
  uuid: ^4.5.1
  device_info_plus: ^11.2.0
  local_auth: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  generate: true
